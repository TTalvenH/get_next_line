#include "get_next_line.h"

char *get_next_line(int fd)
{
	void	*buff;
	
}

int main()
{
	int fd
	open("text.txt", O_RDONLY);
	for (int i = 0, i < 4, i++)
	{
		printf("%s", get_next_line(fd));
	}
	return (0)
}